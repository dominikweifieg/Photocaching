<h2>New Page</h2>

<% form_for(@page) do |f| %>
  <%= f.error_messages %>
	<%= render :partial => 'editor' , :locals => {:f => f} %>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', pages_path %>
